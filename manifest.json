{
  "schemaVersion": 1,
  "id": "njpatel.omaherdr",
  "name": "Omaherdr",
  "version": "1.0.0",
  "author": "Neil Patel",
  "license": "Apache-2.0",
  "description": "TUI-style panel for herdr: every session you are attached to from this desktop, its spaces, tabs and agents with live status, and one keypress to jump to any of them.",
  "preview": "assets/omaherdr.png",
  "kinds": [
    "bar-widget"
  ],
  "activation": "on-demand",
  "entryPoints": {
    "barWidget": "Widget.qml"
  },
  "barWidget": {
    "displayName": "Omaherdr",
    "description": "Agents waiting for input or done in the bar; a panel listing spaces, tabs and agents across every attached herdr (local or --remote) that jumps to the right terminal window and target.",
    "category": "AI",
    "aliases": [
      "omaherdr",
      "herdr"
    ],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "barMetric": "attention",
      "barIcon": "robot",
      "view": "agents",
      "barStyle": "lights",
      "scanIntervalSec": 10,
      "preferTerminal": "auto"
    },
    "schema": [
      {
        "key": "barMetric",
        "type": "enum",
        "label": "Lights shown beside the icon",
        "options": [
          "attention",
          "active",
          "all",
          "none"
        ],
        "defaultValue": "attention",
        "description": "attention = red (needs input) and green (done); active = also yellow (working); all = also grey (idle); none = icon only, with the lit lights stacked beside it. Counts are hidden when zero."
      },
      {
        "key": "barStyle",
        "type": "enum",
        "label": "How the lights are drawn",
        "options": [
          "lights",
          "inverse"
        ],
        "defaultValue": "lights",
        "description": "lights = a small square beside each count; inverse = the count on a pill in the state's colour (icon-only mode paints the colours behind the icon)."
      },
      {
        "key": "barIcon",
        "type": "string",
        "label": "Bar icon",
        "defaultValue": "robot",
        "description": "A name (robot, robot-happy, console, terminal, brain, group, dashboard, flag, bell, server, lan, chip, pulse) or any Nerd Font glyph."
      },
      {
        "key": "view",
        "type": "enum",
        "label": "Panel view",
        "options": [
          "agents",
          "spaces"
        ],
        "defaultValue": "agents",
        "description": "agents = one row per agent, most urgent first; spaces = every space with its tabs."
      },
      {
        "key": "scanIntervalSec",
        "type": "integer",
        "label": "Seconds between scans for herdr sessions",
        "min": 2,
        "max": 60,
        "step": 1,
        "defaultValue": 10,
        "description": "How often the daemon looks for herdr clients starting or stopping. Agent status itself is pushed by herdr, so this only affects how quickly a new or closed session shows up."
      },
      {
        "key": "preferTerminal",
        "type": "string",
        "label": "Preferred terminal for jumps",
        "defaultValue": "auto",
        "description": "When more than one terminal window hosts the same herdr server, jump to the one whose window class contains this (kitty, foot, alacritty, ghostty, wezterm). auto = pick by herdr's window title, then the current workspace, then most recent focus."
      }
    ]
  }
}
